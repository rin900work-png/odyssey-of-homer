# Журнал сессий — Одиссея Гомера

### [2026-07-01]
- Отделили проект от `wb_sales`: у Одиссеи теперь свой `CLAUDE.md`, своя папка `brain/` и своя
  память Claude (`~/.claude/projects/-Users-rinatismagulov-Documents-odyssey-of-homer/memory/`).
- Проверили: на уровне кода/git проекты и так были раздельны — разные папки, разные git-remote,
  общих файлов/зависимостей нет. Отделяли рабочий контекст Claude, а не код.
- Дальше: вести проект самостоятельно из этого репозитория. Цикл правок:
  `content.py` → `python3 build.py` → коммит (в один коммит идут исходники и пересобранный `docs/`).
- Добавили **режим чтения**: полный текст всех 24 песен (перевод Жуковского, PD).
  - Источник — Викитека (ru.wikisource.org), `action=raw`; скачали скриптом, вычистили вики-разметку
    (`{{nr}}`, `{{разрядка}}`, сноски). Итог — 12 106 стихов, по одному файлу `texts/song-NN.txt`.
  - `build.py`: `read_verses()` + `render_reader()` — раскрывающийся `<details>`, строфы по 10 строк,
    номер стиха каждые 10 (охра в левом поле). Ударения (U+0301) сохранены как в источнике.
  - CSS: тема чтения (Georgia, пергамент, интерлиньяж 1.85, висячий отступ переносов). Скриншот — ок.
  - Проверено headless-Chrome: reader на всех 24 страницах, нумерация 1/10/20…, разметки не осталось.
